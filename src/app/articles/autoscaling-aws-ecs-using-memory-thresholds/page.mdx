import { ArticleLayout } from '@/components/ArticleLayout'

export const article = {
        author: 'Greg Macbean',
        date: '2022-03-15',
        title: 'Autoscaling AWS ECS using memory thresholds',
        description:
                `In this example we will add autoscaling based on memory thresholds to our ECS Cluster`,
        tags: ['examples'],
}

export const metadata = {
        title: article.title,
        description: article.description,
}

export default (props) => <ArticleLayout article={article} {...props} />


